Wellwell is a template catalog for Interchange. It's main difference from previous IC demo catalogs that it doesn't tend to be a demo, but rather a frame for building new catalogs. 

It's main building blocks are components, assembled together with [compose] called from within pages.

Users
*****

By default, Wellwell comes with two user roles:

    * Anonymous user: this role is used for users that don't have a user account or that are not authenticated.
    * Authenticated user: this role is automatically granted to all logged in users.

Automatic components/attributes
*******************************

MV_COMPONENT_AUTO
MV_ATTRIBUTE_AUTO

